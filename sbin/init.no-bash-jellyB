#!/sbin/busybox sh
export _PATH="$PATH"
export PATH=/sbin

busybox cd /
busybox rm init

rm -f /init
rm -f /init.samsung
rm -f /init.cm9
rm -f /update*
mv /init.jb /init

export PATH="${_PATH}"
cd /
exec /init
