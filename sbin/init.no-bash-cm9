#!/sbin/busybox sh
export _PATH="$PATH"
export PATH=/sbin

busybox cd /
busybox rm init

rm -f /init
rm -f /init.samsung
rm -f /init.jb
rm -f /update*
rm -f init.cm10
mv /init.cm9 /init

export PATH="${_PATH}"
cd /
exec /init

