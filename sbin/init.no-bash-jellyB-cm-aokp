#!/sbin/busybox sh
export _PATH="$PATH"
export PATH=/sbin

busybox cd /
busybox rm init

rm -f /init
rm -f /init.samsung
rm -f /update*
rm -f init.jb
rm -f init.cm9
mv /init.cm10 /init

export PATH="${_PATH}"
busybox rm -fr /dev/*
cd /
exec /init

