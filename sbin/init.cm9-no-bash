#!/sbin/busybox sh
export _PATH="$PATH"
export PATH=/sbin

busybox cd /
busybox date >>boot.txt
exec >>boot.txt 2>&1

rm -f /init
rm -f /init.samsung
mv /init.cm9 /init
busybox cp -a /res/misc/cyano/* /

busybox date >>boot.txt
busybox rm -rf /res/misc/cyano /dev/*
export PATH="${_PATH}"
exec /init

