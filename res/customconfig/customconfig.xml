<settingsRoot>
<settingsTab name="CPU">

		<settingsPane description="The following settings control CPU hotplug parameters. Depending on the following parameters, the second core is enabled or disabled." name="CPU Hotplug Settings">

			<spinner description="Sets the CPU Hotplug Mode. Hotplug mode will dynamically switch the second core on and off. Using second core always-off or always-on modes will make the other hotplug parameters useless." name="Hotplug Mode" action="secondcore">
				<spinnerItem name="CPU Hotplug (default)" value="hotplug"/> 
				<spinnerItem name="Second core always-off" value="always-off"/> 
				<spinnerItem name="Second core always-on" value="always-on"/> 
			</spinner>

		</settingsPane>

		<settingsPane description="The following settings are for all global CPU configuration, use it only if you know what you are doing." 
			name="CPU Global Settings">

			<spinner description="This setting will change CPU step count (Restart required)." 
				name="CPU Step Count" action="generic /tmp/cpustepcount">
				<spinnerItem name="6 (1200-1000-800-500-200-100MHz)" value="6"/> 
				<spinnerItem name="7 (1400->100-7-Steps)" value="7"/> 
				<spinnerItem name="8 (1500->100-8-Steps)" value="8"/> 
				<spinnerItem name="9 (1500->100-9-Steps)" value="9"/> 
				<spinnerItem name="15 (1500->100-15-Steps)" value="15"/> 
			</spinner>

			<seekBar description="Maximum CPU Speed (default is 1200)" 
				name="scaling_max_freq" action="generic /sys/devices/system/cpu/cpu0/cpufreq/scaling_max_freq" 
					unit="kHz" min="100000" reversed="false" step="100000" max="1500000"/>

			<seekBar description="Minimum CPU Speed (default is 200)" 
				name="scaling_min_freq" action="generic /sys/devices/system/cpu/cpu0/cpufreq/scaling_min_freq" 
					unit="kHz" min="100000" reversed="false" step="100000" max="1500000"/>

			<spinner description="This sets the smooth scaling level which prevents ondemand based governors to jump directly to maximum frequency, with load spikes. This setting will only change the behavior of ondemand and pegasusq governors." 
				name="Smooth Scaling Level" action="generickmem smooth_level 0 int">
				<spinnerItem name="1500MHz" value="1"/> 
				<spinnerItem name="1200MHz" value="4"/> 
				<spinnerItem name="1000MHz" value="6"/> 
				<spinnerItem name="800MHz" value="8"/> 
				<spinnerItem name="500MHz" value="11"/> 
				<spinnerItem name="200MHz" value="14"/> 
				<spinnerItem name="100MHz" value="15"/> 
			</spinner>

			<spinner description="This will set the AWAKE (screen ON) CPU governor. Default CPU governor will only be set during early stages of booting (before init.d execution), and can be overridden by later scripts or apps." 
				name="Default CPU Governor" action="generic /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor">
				<spinnerItem name="HYPER" value="HYPER"/> 
				<spinnerItem name="pegasusq" value="pegasusq"/> 
				<spinnerItem name="abyssplug" value="abyssplug"/> 
				<spinnerItem name="lulzactive" value="lulzactive"/> 
				<spinnerItem name="ondemand" value="ondemand"/> 
				<!--spinnerItem name="smartassV2" value="smartassV2"/--> 
				<spinnerItem name="conservative" value="conservative"/> 
				<spinnerItem name="scary" value="scary"/> 
			</spinner>

			<seekBar description="second core will be enabled when the load goes above this threshold (default is 30)" 
				name="load_h0" action="generic /sys/module/stand_hotplug/parameters/load_h0" unit="%" min="5" reversed="false" step="1" max="70"/>

			<seekBar description="second core will be disabled when the load goes below this threshold (default is 20)" 
				name="load_l1" action="generic /sys/module/stand_hotplug/parameters/load_l1" unit="%" min="5" reversed="false" step="1" max="70"/>

			<seekBar description="(default is 2)" 
				name="min_rq" action="generic /sys/module/stand_hotplug/parameters/min_rq" unit="" min="0" reversed="false" step="1" max="2"/>

			<seekBar description="(default is 20)" 
				name="load_rq" action="generic /sys/module/stand_hotplug/parameters/load_rq" unit="%" min="5" reversed="false" step="1" max="35"/>

			<seekBar description="Sets hot plug sampling intervals for screen-on state (default is 100)" 
				name="rate" action="generic /sys/module/stand_hotplug/parameters/rate" unit=" jiffies" min="100" reversed="false" step="100" max="1000"/>

			<seekBar description="Sets the frequency below which second core will not be turned on, regardless of thresholds. (default: 100000)" 
				name="freq_min" action="generic /sys/module/stand_hotplug/parameters/freq_min" 
				unit="kHz" min="0" reversed="false" step="100000" max="1200000"/>

		</settingsPane>

		<settingsPane description="These settings control CFS (Complete Fair Scheduler) Task Scheduler Settings" name="CFS Settings">

		<checkbox description="Disabling GENTLE_FAIR_SLEEPERS may give you better UI responsiveness." 
			name="GENTLE_FAIR_SLEEPERS" action="schedfeat GENTLE_FAIR_SLEEPERS" label="GENTLE_FAIR_SLEEPERS"/>

		<checkbox description="Using ARCH_POWER enables the use of arm topology power functions instead of generic ones." 
			name="ARCH_POWER" action="schedfeat ARCH_POWER" label="ARCH_POWER"/>

		</settingsPane>

		<settingsPane description="Here you can set all the settings for power save!" 
			name="CPU Idle and Power Settings">

		<spinner description="Sets the CPU undervolting value which will be set during boot time only. 'ALERT -100 and -75 can brake the boot or stuck the CPU, i have UV the CPU already by default! so dont post about it if you stuck!' It will not have any effect until you restart your device. Remember: if you change this value and your device doesn't boot you will have to reset extweaks settings using CWM recovery. This value will be applied to all CPU steps before any script or app execution. If you use any script or voltage control app this setting may be overridden by those." 
			name="CPU Undervolting" action="generic /tmp/cpu_undervolting">
			<spinnerItem name="No Undervolting" value="0"/> 
			<spinnerItem name=" -25 mV" value="-25"/> 
			<spinnerItem name=" -50 mV" value="-50"/> 
			<spinnerItem name=" -75 mV" value="-75"/> 
			<spinnerItem name="-100 mV" value="-100"/> 
		</spinner>

		<spinner description="Sets the CPU Idle Mode. AFTR+LPA is the default and best for the battery life but if you have problems try Idle+LPA." 
			name="CPU Idle Mode" action="generic /sys/module/cpuidle_exynos4/parameters/enable_mask">
			<spinnerItem name="AFTR + LPA (default)" value="3"/> 
			<spinnerItem name="Idle + LPA" value="2"/> 
			<spinnerItem name="AFTR only" value="1"/> 
			<spinnerItem name="Idle only" value="0"/> 
		</spinner>

		<seekBar description="Sets the sched_mc value which changes the cpu scheduler behavior for multi-cpu systems. 0 is Samsung kernel's default value which turns sched_mc off. Setting it to 1 means that the first core will be fully loaded before SEC CPU will start, and setting it to 2 will bias task wake-ups to semi-idle cpu package for power savings. (default: 0)" 
			name="sched_mc" action="generic /sys/devices/system/cpu/sched_mc_power_savings" unit="" min="0" reversed="false" step="1" max="2"/>

		<seekBar description="This is for CPU BUS-FREQ UP-FREQ-Scaling, based on CPU load! default=30%" 
			name="busfreq_up_threshold" action="generic /sys/devices/system/cpu/cpufreq/busfreq_up_threshold" 
			unit="%" min="23" reversed="false" step="1" max="50"/>

		<seekBar description="This is for CPU BUS-FREQ DOWN-FREQ-Scaling, based on CPU load! default=30%" 
			name="busfreq_down_threshold" action="generic /sys/devices/system/cpu/cpufreq/busfreq_down_threshold" 
			unit="%" min="23" reversed="false" step="1" max="50"/>

		<seekBar description="Maximum DEEP SLEEP CPU Speed (only 500 or 600 default is 600)" 
			name="scaling_max_suspend_freq" action="generic /sys/devices/system/cpu/cpu0/cpufreq/scaling_max_suspend_freq" 
			unit="kHz" min="500000" reversed="false" step="100000" max="600000"/>

		<seekBar description="Minimum DEEP SLEEP CPU Speed (default is 200)" 
			name="scaling_min_suspend_freq" action="generic /sys/devices/system/cpu/cpu0/cpufreq/scaling_min_suspend_freq" 
			unit="kHz" min="100000" reversed="false" step="100000" max="600000"/>

		<seekBar description="Minimum Standby (music palying on screen off) CPU Speed, (default is 100kHz) if music still shutter, rise to 200/300kHz"
			name="scaling_min_standby_freq" action="generic /tmp/standby_freq"
			unit="kHz" min="100000" reversed="false" step="100000" max="300000"/>

		<spinner description="This will set the DEEP-SLEEP (screen OFF) CPU governor, depend on cortex background process ON." 
			name="Deep-Sleep! CPU Governor" action="generic /tmp/deep_sleep">
			<spinnerItem name="ondemand" value="ondemand"/> 
			<spinnerItem name="scary" value="scary"/>
			<spinnerItem name="conservative" value="conservative"/> 
			<spinnerItem name="pegasusq" value="pegasusq"/> 
			<spinnerItem name="abyssplug" value="abyssplug"/> 
			<!--spinnerItem name="smartassV2" value="smartassV2"/--> 
			<spinnerItem name="lulzactive" value="lulzactive"/> 
			<spinnerItem name="HYPER" value="HYPER"/> 
		</spinner>

	</settingsPane>

	</settingsTab>

<settingsTab name="GPU">
	<settingsPane description="GPU Frequency Settings. Normally, our device has 2 frequency steps. 
This Kernel has three steps but the third step is not enabled as default. The defaults are 160,267 and 267. 
Voltage defaults are 950mV, 1000mV and 1000mV." name="GPUFreq">

	<spinner description="Changes the first (lowest) GPU frequency step" name="GPU Freq Step1" action="gpu-freq 1">
		<spinnerItem name="400MHz" value="400"/> 
		<spinnerItem name="267MHz" value="267"/> 
		<spinnerItem name="200MHz" value="200"/> 
		<spinnerItem name="160MHz" value="160"/> 
		<spinnerItem name="133MHz" value="133"/> 
		<spinnerItem name="100MHz" value="100"/> 
		<spinnerItem name="66MHz" value="66"/> 
		<spinnerItem name="40MHz" value="40"/> 
	</spinner>

	<spinner description="Changes the second (middle) GPU frequency step" name="GPU Freq Step2" action="gpu-freq 2">
		<spinnerItem name="400MHz" value="400"/> 
		<spinnerItem name="267MHz" value="267"/> 
		<spinnerItem name="200MHz" value="200"/> 
		<spinnerItem name="160MHz" value="160"/> 
		<spinnerItem name="133MHz" value="133"/> 
		<spinnerItem name="100MHz" value="100"/> 
		<spinnerItem name="66MHz" value="66"/> 
		<spinnerItem name="40MHz" value="40"/> 
	</spinner>

	<spinner description="Changes the third (last) GPU frequency step" name="GPU Freq Step3" action="gpu-freq 3">
		<spinnerItem name="400MHz" value="400"/> 
		<spinnerItem name="267MHz" value="267"/> 
		<spinnerItem name="200MHz" value="200"/> 
		<spinnerItem name="160MHz" value="160"/> 
		<spinnerItem name="133MHz" value="133"/> 
		<spinnerItem name="100MHz" value="100"/> 
		<spinnerItem name="66MHz" value="66"/> 
		<spinnerItem name="40MHz" value="40"/> 
	</spinner>

	</settingsPane>

	<settingsPane description="GPU Voltage Levels: The defaults are 950,1050 and 1050." name="GPU Voltage Levels">

		<seekBar description="GPU Voltage for the first (lowest) step" 
			name="GPU Voltage Level1" action="gpu-voltage 1" unit="mV" min="800" reversed="false" step="50" max="1200"/>

		<seekBar description="GPU Voltage for the second (middle) step" 
			name="GPU Voltage Level2" action="gpu-voltage 2" unit="mV" min="800" reversed="false" step="50" max="1200"/>

		<seekBar description="GPU Voltage for the third (highest) step" 
			name="GPU Voltage Level3" action="gpu-voltage 3" unit="mV" min="800" reversed="false" step="50" max="1200"/>

	</settingsPane>

	<settingsPane description="GPU load thresholds to change between frequency steps. 
The third step is disabled as default by having the second to third threshold 100." name="GPU Thresholds">

		<seekBar description="GPU threshold to jump up from first to second step" 
			name="GPU Threshold 1-UP" action="gpu-thresh 1UP" unit="%" min="0" reversed="false" step="5" max="100"/>

		<seekBar description="GPU threshold to jump down from second to first step" 
			name="GPU Threshold 2-DOWN" action="gpu-thresh 2DOWN" unit="%" min="0" reversed="false" step="5" max="100"/>

		<seekBar description="GPU threshold to jump up from second to third step" 
			name="GPU Threshold 2-UP" action="gpu-thresh 2UP" unit="%" min="0" reversed="false" step="5" max="100"/>

		<seekBar description="GPU threshold to jump down from third to second step" 
			name="GPU Threshold 3-DOWN" action="gpu-thresh 3DOWN" unit="%" min="0" reversed="false" step="5" max="100"/>

	</settingsPane>

	<settingsPane description="The number of cycles that the GPU stays at the specific level. The defaults are all 0" name="GPU StayCount">

		<seekBar description="GPU first (for the lowest freq) StayCount" 
			name="GPU StayCount1" action="gpu-staycount 1" unit=" sec" min="0" reversed="false" step="1" max="5"/>

		<seekBar description="GPU second (for the middle freq) StayCount" 
			name="GPU StayCount2" action="gpu-staycount 2" unit=" sec" min="0" reversed="false" step="1" max="5"/>

		<seekBar description="GPU third (for the highest freq) StayCount" 
			name="GPU StayCount3" action="gpu-staycount 3" unit=" sec" min="0" reversed="false" step="1" max="5"/>

	</settingsPane>

</settingsTab>

<settingsTab name="Screen">

	<settingsPane description="Brightness Curve settings which modify auto-brightness behavior." name="Brightness Curve">

	<seekBar description="Sets touch screen move sensitivity (vitalij value)." 
		name="TouchMove Sensitivity" action="generic /sys/devices/platform/s3c2440-i2c.3/i2c-3/3-004a/mov_hysti" 
		unit=" pixel" min="0" reversed="false" step="1" max="20"/>

	<seekBar description="Setting min_bl higher will result in staying lowest brightness level for brighter conditions (default 30)" 
		name="min_bl" action="generic /sys/class/misc/brightness_curve/min_bl" unit="" min="0" reversed="false" step="10" max="150"/>

	<seekBar description="Minimum brightness level (default 0)" 
		name="min_gamma" action="generic /sys/class/misc/brightness_curve/min_gamma" unit="" min="0" reversed="false" step="1" max="24"/>

	<seekBar description="Maximum brightness level (default 24)" 
		name="max_gamma" action="generic /sys/class/misc/brightness_curve/max_gamma" unit="" min="0" reversed="false" step="1" max="24"/>

	</settingsPane>

	<settingsPane description="Settings for screen color by user manual input" name="Screen Color Menu">

	<button description="Push to reset screen color to default" name="Reset Color to Default" action="push-actions/color_to_default 1" label="Default Color"/>

	<seekBar description="Sets screen BLUE color scale (Default is 135)" 
		name="Blue Color Scale" action="generic /sys/devices/platform/samsung-pd.2/mdnie/mdnie/mdnie/user_cb" 
		unit="BLUE" min="0" reversed="false" step="1" max="244"/>

	<seekBar description="Sets screen RED color scale (Default is 120)" 
		name="Red Color Scale" action="generic /sys/devices/platform/samsung-pd.2/mdnie/mdnie/mdnie/user_cr" 
		unit="RED" min="0" reversed="false" step="1" max="244"/>

	</settingsPane>

	<settingsPane description="Other Settings" name="Other Settings">

	<seekBar description="User adjustable gamma shift." 
		name="Gamma Shift" action="generic /sys/devices/platform/samsung-pd.2/s3cfb.0/spi_gpio.3/spi_master/spi3/spi3.0/lcd/panel/user_gamma_adjust" 
		unit="" min="-50" reversed="false" step="5" max="50"/>

	<seekBar description="Vibration force level (default 75)" 
		name="Vibration Force" action="generic /sys/vibrator/pwm_val" unit="%" min="25" reversed="false" step="25" max="100"/>

	<seekBar description="Sets Touch screen cpu freq, when you touch the screen CPU will be boosted to that speed! if you are using luzactive gov set this to max CPU you like to use! or it's will be stuck on this limit! default is 500000mHz)" 
		name="Touch screen lock freq" action="generic /sys/devices/virtual/sec/sec_touchscreen/tsp_touch_freq" 
		unit="mHz" min="200000" reversed="false" step="100000" max="1500000"/>

	<checkbox description="Enable inverting the screen colors by quickly pressing Home button below configured times" 
		name="mDNIe Negative Toggle" action="generic01 /sys/module/gpio_keys/parameters/mdnie_shortcut_enabled" label="mDNIe Negative Toggle"/>

	<spinner description="Negative Color, Screen effect settings, Fast Click MENU for number of times configured to activate MOD" 
		name="Negative Screen Mod tuning." action="negative_tweak">
		<spinnerItem name="Set 4 Clicks (default)" value="1"/> 
		<spinnerItem name="Set 6 Clicks" value="2"/> 
	</spinner>

	</settingsPane>

        <settingsPane description="Touch screen configuration on BATT and on CHARGE, !ALERT! wrong set will disable touch screen! 
and you will have too reset profiles in recovery! Also settings for Battery VS Charger are different! 
do not set the same levels! have fun tuning." name="Touch Screen Sensitivity">

                <seekBar description="Touch Screen on BATTERY (Touch Treshold) (default=50)" 
                        name="BATT Touch Treshold" action="touch_screen_tweak 1" unit="" min="30" reversed="false" step="2" max="70"/>

                <seekBar description="Touch Screen on BATTERY (Noise Treshold) (default=40)" 
                        name="BATT Noise Treshold" action="touch_screen_tweak 2" unit="" min="20" reversed="false" step="2" max="70"/>

                <seekBar description="Touch Screen on BATTERY (Move Filter) (default=14)" 
                        name="BATT Move Filter" action="touch_screen_tweak 3" unit="" min="2" reversed="false" step="2" max="30"/>

                <seekBar description="Touch Screen on CHARGER/USB (Touch Treshold) (default=53)" 
                        name="CHRG Touch Treshold" action="touch_screen_tweak 4" unit="" min="30" reversed="false" step="2" max="70"/>

                <seekBar description="Touch Screen on CHARGER/USB (Noise Treshold) (default=38)" 
                        name="CHRG Noise Treshold" action="touch_screen_tweak 5" unit="" min="20" reversed="false" step="2" max="60"/>

                <seekBar description="Touch Screen on CHARGER/USB (Move Filter) (default=46)" 
                        name="CHRG Move Filter" action="touch_screen_tweak 6" unit="" min="20" reversed="false" step="2" max="60"/>

        </settingsPane>

</settingsTab>

<settingsTab name="Audio+Swipes">

	<settingsPane description="Headphone Amplification Settings Thanks to Gokhanmoral!" name="Headphone Amplification">

	<seekBar description="Headphone Amplifier Level (0-3 which corresponds to 0-1.5-3-6dB)"
                name="Headphone Amplifier" action="soundgasm_hp" unit="" min="0" reversed="false" step="1" max="3"/>

	</settingsPane>

	<settingsPane description="Here you can control kernel screen gestures, set at /data/gesture_set.sh, more will be added later, this tweak depend on cortex background service ON" name="Gestures Panel">

	<checkbox description="Here you can turn on/off kernel build in gestures! see OP for instructions/ask in forum." 
		name="Gestures switch" action="generic_gestures /tmp/gesture_tweak" label="GESTURES SWITCH"/>


	<button description="Click to reset gestures to default, or update with new one from dev, gesture file will be added to `/data/` only once if was missing." name="RESET GESTURES" action="push-actions/reset_gestures" label="Reset Gestures"/>

	</settingsPane>

</settingsTab>

<settingsTab name="BLN">

	<settingsPane description="Back Light Notification Settings" name="Back Light Notification(BLN)">

		<checkbox description="BackLightNotification Switch" 
			name="BLN" action="generic01 /sys/class/misc/backlightnotification/enabled" label="BLN"/>

		<checkbox description="Back Light Notification will work when on Charger/USB if above switch is OFF or ON"
			name="BLN on Charger" action="generic01 /sys/class/misc/backlightnotification/enabled_charging" 
				label="BLN Enabled on charging"/>

		<spinner description="Back Light Notification switch, will work on Battery/Charger/USB" 
			name="BLN MODE Switch" action="bln_switch">
			<spinnerItem name="Active and static" value="1"/> 
			<spinnerItem name="Active and breathing" value="2"/> 
			<spinnerItem name="Active and blinking" value="3"/> 
		</spinner>

		<spinner description="Notification timeout. -1: disabled" 
			name="Notification Timeout" action="generic /sys/class/misc/backlightnotification/notification_timeout">
			<spinnerItem name="Never timeout" value="-1"/> 
			<spinnerItem name="1 Minutes" value="60000"/> 
			<spinnerItem name="3 Minutes" value="180000"/> 
			<spinnerItem name="5 Minutes" value="300000"/> 
			<spinnerItem name="10 Minutes" value="600000"/> 
			<spinnerItem name="15 Minutes" value="900000"/> 
			<spinnerItem name="30 Minutes" value="1800000"/> 
			<spinnerItem name="1 Hour" value="3600000"/> 
			<spinnerItem name="2 Hours" value="7200000"/> 
			<spinnerItem name="5 Hours" value="18000000"/> 
		</spinner>

		<spinner description="This setting will set renew notification timeout yes/no." 
			name="BLN Notification Reset" action="bln_reset_notification">
			<spinnerItem name="Don't renew BLN timeout" value="0"/> 
			<spinnerItem name="Renew BLN timeout" value="1"/> 
		</spinner>

		<spinner description="This setting will set BL Notification always ON, with charger, OFF/ON." 
			name="BLN Notification on charger" action="bln_charging_notification">
			<spinnerItem name="BLN no timeout OFF, on charger" value="0"/> 
			<spinnerItem name="BLN no timeout ON, on charger" value="1"/> 
		</spinner>

		<spinner description="Back Light Notification OFF on low battery or ignore battery level" name="BLN Battery Check" action="bln_check_battery">
			<spinnerItem name="BLN Ignore Battery level" value="0"/> 
			<spinnerItem name="BLN OFF if 5% battery" value="1"/> 
			<spinnerItem name="BLN OFF if 10% battery" value="2"/> 
			<spinnerItem name="BLN OFF if 15% battery" value="3"/> 
			<spinnerItem name="BLN OFF if 20% battery" value="4"/> 
			<spinnerItem name="BLN OFF if 25% battery" value="5"/> 
			<spinnerItem name="BLN OFF if 30% battery" value="6"/> 
		</spinner>

		<button description="Use this button to test bln. BLN will be active after you turn the screen off." name="Test BLN" action="push-actions/test_bln" label="Test BLN"/>

		</settingsPane>

		<settingsPane description="Touch Keys Light Settings, here you can set the way touch keys light work" 
			name="Touch Keys Lights Settings">

		<spinner description="Led timeout. Leave it disabled if your ROM already has led timeout support. In CM9, if you have lights enabled screen brightness changes may trigger lights on. It is normal. If you want them to stay disabled you have to disable leds in CM9 menu." 
			name="Keys led timeout" action="led_timeout">
			<spinnerItem name="Always OFF" value="off"/> 
			<spinnerItem name="1 sec" value="1"/> 
			<spinnerItem name="2 sec" value="2"/> 
			<spinnerItem name="3 sec" value="3"/> 
			<spinnerItem name="5 sec" value="5"/> 
			<spinnerItem name="15 sec" value="15"/> 
			<spinnerItem name="30 sec" value="30"/> 
			<spinnerItem name="Always ON" value="on"/> 
		</spinner>

		<checkbox description="Selecting this will activate turning touckey leds on when you touch the screen (only when led timeout is enabled) value from led timeout will be used for this mod."
			name="Leds on with touch" action="generic_touch_led /sys/devices/virtual/sec/sec_touchscreen/tsp_flash_timeout" label="led-on-touch"/>

		<checkbox description="Led fadeout effect. If you are on a Samsung ROM, you need to set touch keys leds to always on or always off, and use led timeout in ExTweaks." name="led-fadeout" action="generic01 /sys/class/misc/backlightnotification/led_fadeout" label="Led Fadeout effect"/>

		<seekBar description="Controls touchpad led voltage level (default: 3000)" 
			name="Led Voltage Level" action="generic /sys/devices/virtual/sec/sec_touchkey/touchkey_brightness" 
			unit=" mV" min="2500" reversed="false" step="50" max="3300"/>

		</settingsPane>

</settingsTab>

<settingsTab name="Misc">

	<settingsPane description="Dual Mount-SDCARD and MMC via USB after USB disconnected Dual Mount Disabled!" name="DUAL MOUNT USB">

		<button description="Dual Mount USB Switch" name="DUAL MOUNT" action="push-actions/dualmount" label="Dual Mount"/>

	</settingsPane>

	<settingsPane description="Here you can run Fix Permissions, Zipalign, DB Optimization script's with one click! please click one time only, scripts will finish in 2~3Min max, no reboot needed." name="FIX PERMISIONS + ZIPALIGN + DB OPTIMIZE">

		<button description="Click here to start fixing." name="FIX PERMISSIONS" action="push-actions/fix_permissions" label="FIX PERMISSIONS"/>

		<button description="Click here to start Zipalign all system and user apps, system may be unresponsive, it's OK, do not use phone for 2Min." name="ZIPALIGN" action="push-actions/zipalign" label="ZIPALIGN ALL APPS"/>

		<button description="Click here to start Apps DataBase's Optimization, compacting, cleaning and reindexing, will take 2min max." name="DB OPTIMIZATION" action="push-actions/database_optimizing" label="DATABASE OPTIMIZATON"/>

	</settingsPane>

	<settingsPane description="Misc Settings" name="Misc Settings">

		<spinner description="WIFI supplicant scan interval, here you can set the time in sec for WIFI to look for access points when ON, Higher the range, less battery used by WIFI! but if you want WIFI to be off on screen OFF, and you set it in ROM settings!, then set it 60sec, if cortex process ON then no problem, will be set by auto and set your setting on screen ON to 360sec and off to 60sec!"
			name="WIFI supplicant scan interval" action="supplicant_scan_interval">
			<spinnerItem name="Set WIFI SCAN 60Sec" value="60"/> 
			<spinnerItem name="Set WIFI SCAN 120Sec" value="120"/> 
			<spinnerItem name="Set WIFI SCAN 180Sec" value="180"/> 
			<spinnerItem name="Set WIFI SCAN 240Sec" value="240"/> 
			<spinnerItem name="Set WIFI SCAN 360Sec" value="360"/> 
		</spinner>

		<checkbox description="Enable or Disable Android Logger (needs restart)" 
			name="Android Logger" action="generic /tmp/logger" label="Android Logger"/>

		<spinner description="This setting enables/disables the BootMenu and logo NOTE: You will not be able to boot the second rom while BootMenu is disabled" name="Enable Bootmenu" action="bootmenu_enabled"> 
			<spinnerItem name="BootMenu Enabled" value="1"/> 
			<spinnerItem name="BootMenu Disabled" value="0"/> 
		</spinner>

		<spinner description="ZRAM Module settings, here you can shutdown ZRAM or add more compressed RAM to System use." 
			name="ZRAM Settings" action="zramtweaks">
			<spinnerItem name="Set ZRAM to 150MB" value="1"/> 
			<spinnerItem name="Set ZRAM to 300MB (default)" value="2"/> 
			<spinnerItem name="Set ZRAM to 600MB" value="3"/> 
			<spinnerItem name="Set ZRAM OFF!" value="4"/> 
		</spinner>

		<button description="ZRAM check for active state" name="ZRAM STATS" action="push-actions/zramstats" label="ZRAM STATS"/>

		<spinner description="GPS Time Zone for faster LOCK! Use your region to fix GPS location!" name="GPS Region" action="gpsregion">
			<spinnerItem name="No GPS Zone changes (default)" value="No_GPS_Zone_changes"/> 
			<spinnerItem name="Set Asia" value="Asia"/> 
			<spinnerItem name="Set Europe" value="Europe"/> 
			<spinnerItem name="Set Oceania" value="Oceania"/> 
			<spinnerItem name="Set North America" value="North-America"/> 
			<spinnerItem name="Set South America" value="South-America"/> 
			<spinnerItem name="Set Africa" value="Africa"/> 
			<spinnerItem name="Restore original ROM gps.conf" value="Original_ROM_GPS_Restored"/> 
		</spinner>

		<button description="Click to see your current GPS ZONE" name="GPS ZONE" action="push-actions/gpszonecheck" label="GPS ZONE CHECK"/>

		<spinner description="This will set the default I/O scheduler. Default I/O scheduler will only be set during early stages of booting (before init.d execution), and can be overridden by later scripts or apps." 
			name="Default I/O Scheduler" action="generic /sys/block/mmcblk0/queue/scheduler">
			<spinnerItem name="sio" value="sio"/> 
			<spinnerItem name="noop" value="noop"/> 
			<spinnerItem name="deadline" value="deadline"/> 
			<spinnerItem name="vr" value="vr"/> 
			<spinnerItem name="cfq" value="cfq"/> 
			<spinnerItem name="bfq" value="bfq"/> 
		</spinner>

		<spinner description="Change USB connection mode. Either leave it as default or force it to mass storage mode (you will see your sdcards as disks) or mtp" 
			name="USB Mass Storage Mode" action="usb-mode">
			<spinnerItem name="Default" value="default"/>
			<spinnerItem name="Force Mass Storage Mode" value="mass_storage-adb"/>
			<spinnerItem name="Force MTP Mode" value="mtp-adb"/>
		</spinner>

		<checkbox description="If checked, BetterBatteryStatus will report exclusive (non-overlapping) wakelock statistics." 
			name="Exclusive Wakelock Stats" action="generic01 /sys/module/wakelock/parameters/default_stats" label="Exclusive Wakelock Stats"/>

		<spinner description="Charge current setting" name="Charge Current" action="chargecurrent">
			<spinnerItem name="AC:650mA, Misc,USB:450mA (Stock)" value="1"/> 
			<spinnerItem name="AC:700mA, Misc:600mA, USB:500mA" value="2"/> 
			<spinnerItem name="AC/Misc:700mA, USB:500mA(risky, may overheat if phone used)" value="3"/> 
		</spinner>

		<button description="If you think that the fuel gauge chip is behaving weird use this button to reset it. Give it a few hours after you reset the gauge. It may still show you funny values for that period, Then the battery percentage should be fine." name="Reset Fuel-Gauge Chip" action="push-actions/reset-fuelgauge" label="Reset Fuel-Gauge Chip"/>

	</settingsPane>

	<settingsPane description="Remove OR Reinstall ROOT, You can have ROOTED or not ROOTED phone." name="Remove OR Reinstall Root">

		<button description="Root Install, Click to install ROOT (SuperUser) + ROM busybox if existed in case you have used the remove button, to Reinstall ROOT first REMOVE it, this function depend on CORTEX Process Enabled!" name="ROOT INSTALL" action="push-actions/root_install" label="INSTALL ROOT"/>

		<button description="Root Remove/Uninstall, Click to remove Any ROOT installed + busybox (kernel busybox remain, but hidden from system) to install ROOT check the Auto-Install Root and REBOOT." name="ROOT REMOVE" action="push-actions/root_remove" label="REMOVE ROOT"/>

		<button description="Root Check, Click to see if you have ROOT or Not" name="ROOT Check" action="push-actions/root_check" label="ROOT CHECK"/>

		<checkbox description="If checked, installs root on the next restart." name="Auto-Install Root" action="generic /tmp/install_root" label="Auto-Install Root"/>

	</settingsPane>

</settingsTab>

<settingsTab name="CortexBrain">

	<settingsPane description="Here you can manage cortexbrain-tune script" name="Automation Script tuning.">

		<spinner description="Enable or Disable the 'background-process' from 'cortexbrain-tune.sh'. If you disable it the cpu-governor will not change automatically for deep-sleep. Battery will not get statistics reset when full/100%, CPU tweaks will not be dynamically changed, use it in your own will/risk. After Enable wait 3sec, then push the `CHECK SERVICE` to see if service is online."
			name="Cortexbrain-Background-Process" action="cortexbrain_background_process">
				<spinnerItem name="Enable" value="1"/> 
				<spinnerItem name="Disable" value="0"/> 
		</spinner>

		<button description="Cortex service checker, Click to see if cortexbrain 'background-process' is running or not" name="CORTEX Service check" action="push-actions/cortexbrain_background_process_check" label="CHECK SERVICE"/>

		<checkbox description="Enable or Disable the 'Wifi turns off while screen is off'-function. [depends on 'cortexbrain_background_process']"
			name="Auto-Wifi" action="generic_cortex /tmp/cortexbrain_auto_tweak_wifi" label="Force Auto-Wifi"/>

		<checkbox description="Enable or Disable the 'Auto-Brightness'-function. [depends on 'cortexbrain_background_process']" 
			name="Auto-Brightness" action="generic_cortex /tmp/cortexbrain_auto_tweak_brightness" label="Force Auto-Brightness"/>

		<checkbox description="Enable or Disable the 'Firewall-Tweaks'. (reboot needed after change)"
			name="Firewall-Tweaks" action="generic_cortex /tmp/cortexbrain_firewall" label="Firewall-Tweaks"/>

		<checkbox description="Enable or Disable the 'TCP-Tweaks'. (reboot needed after change)"
			name="TCP-Tweaks" action="generic_cortex /tmp/cortexbrain_tcp" label="TCP-Tweaks"/>

		<checkbox description="Enable or Disable the 'CPU-Tweaks'. [for auto tune its depends on 'cortexbrain_background_process'] (reboot needed after change)"
			name="CPU-Tweaks" action="generic_cortex /tmp/cortexbrain_cpu" label="CPU-Tweaks"/>

		<checkbox description="Enable or Disable the 'Memory-Tweaks'. (reboot needed after change)"
			name="Memory-Tweaks" action="generic_cortex /tmp/cortexbrain_memory" label="Memory-Tweaks"/>

		<checkbox description="Enable or Disable the 'Battery-Tweaks'. (reboot needed after change)"
			name="Battery-Tweaks" action="generic_cortex /tmp/cortexbrain_battery" label="Battery-Tweaks"/>

		<checkbox description="Enable or Disable the 'System-Tweaks'. (reboot needed after change)"
			name="System-Tweaks" action="generic_cortex /tmp/cortexbrain_system" label="System-Tweaks"/>

		<checkbox description="Enable or Disable the 'Kernel-Tweaks'. (reboot needed after change)"
			name="Kernel-Tweaks" action="generic_cortex /tmp/cortexbrain_kernel_tweaks" label="Kernel-Tweaks"/>

		<checkbox description="Enable or Disable the 'I/O-Tweaks'. (reboot needed after change)"
			name="I/O-Tweaks" action="generic_cortex /tmp/cortexbrain_io" label="I/O-Tweaks"/>

	</settingsPane>

</settingsTab>

<settingsTab name="BACKUP/RESTORE">

	<settingsPane description="Here you can Backup / Restore your EXTWEAKS settings after installing new kernel, when you restore EXTWEAKS app will be closed and your settings will be committed! Some settings will work only after reboot!, wait till EXTWEAKS restart with new settings! restore will take 2MIN, you can lightly use your phone." 
			name="Extweaks Settings Backup / Restore.">

	<button description="Backup Extweaks User Settings!" name="Settings Backup" action="push-actions/config_backup_restore 1" label="BACKUP NOW"/>

	<button description="Restore Extweaks User Settings!" name="Settings Restore" action="push-actions/config_backup_restore 2" label="RESTORE NOW"/>

	</settingsPane>

</settingsTab>

</settingsRoot>
